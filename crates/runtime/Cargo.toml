[package]
name = "runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.51.1", features = ["process", "time"] }

[dev-dependencies]
tokio = { version = "1.51.1", features = ["full"] }
